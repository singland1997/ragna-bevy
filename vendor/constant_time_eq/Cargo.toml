[package]
name = "constant_time_eq"
version = "0.4.2"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Constant-time equality check (local vendored shim for build compatibility)"
repository = "https://example.invalid/"

[lib]
path = "lib.rs"

